package(default_visibility = ["//tests:__subpackages__"])

# Shared helper referenced from the negative-test sub-packages
# (`//tests/error_detection`, `//tests/assert_detection`). Wraps an
# `xsim_test` invocation so a failing xsim exit flips into a passing
# sh_test — the way we assert "this testbench SHOULD fail".
exports_files(["expect_xsim_failure.sh"])
