// Top-level module `top` — depends on `c`, transitively on `b` and `a`.
// The order the leaves land in the project's HDL_SOURCES + XDC_FILES
// lists is what `hdl_order_test.sh` pins.
module top();
endmodule
